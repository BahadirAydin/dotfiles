# Full option list: kitten quick-access-terminal --help

edge right

background_opacity 0.75

# Layer-shell namespace. Default value, restated because the hypr layer_rule
# matches on it and would silently stop blurring if this ever changed.
app_id kitty-quick-access
